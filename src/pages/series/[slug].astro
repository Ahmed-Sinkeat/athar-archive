---
// ponytail: series page retired — redirects to /books
export const prerender = false;
import type { APIRoute } from "astro";
export const GET: APIRoute = ({ params, redirect }) => redirect(`/books`, 301);
---

