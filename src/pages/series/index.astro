---
// ponytail: series retired — redirect to /books; individual درس merged into book
import type { APIRoute } from "astro";
export const GET: APIRoute = ({ redirect }) => redirect("/books", 301);
---
