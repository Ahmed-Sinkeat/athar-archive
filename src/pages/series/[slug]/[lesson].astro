---
// ponytail: lesson reader retired — درس is now a book; redirect to /books
export const prerender = false;
import type { APIRoute } from "astro";
export const GET: APIRoute = ({ redirect }) => redirect("/books", 301);
---
